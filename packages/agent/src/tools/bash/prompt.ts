export const BASH_TOOL_PROMPT =
  'Run a non-interactive shell command inside the workspace. Always provide a concise description, use workdir instead of cd, and expect non-zero exit codes to come back as results instead of tool failures.';
